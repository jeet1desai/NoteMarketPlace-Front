import * as React from "react";
import { Skeleton } from "@mui/material";

const ValueUI = () => {
  return <Skeleton animation="wave" height={20} width={125} style={{ marginBottom: 6 }} />;
};

export default ValueUI;
